import { ListasConceptosSecciones } from './listas-conceptos-secciones';

describe('ListasConceptosSecciones', () => {
  it('should create an instance', () => {
    expect(new ListasConceptosSecciones()).toBeTruthy();
  });
});
