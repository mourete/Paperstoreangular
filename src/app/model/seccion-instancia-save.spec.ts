import { SeccionInstanciaSave } from './seccion-instancia-save';

describe('SeccionInstanciaSave', () => {
  it('should create an instance', () => {
    expect(new SeccionInstanciaSave()).toBeTruthy();
  });
});
