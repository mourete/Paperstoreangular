import { ConceptoInstanciaSave } from './concepto-instancia-save';

describe('ConceptoInstanciaSave', () => {
  it('should create an instance', () => {
    expect(new ConceptoInstanciaSave()).toBeTruthy();
  });
});
