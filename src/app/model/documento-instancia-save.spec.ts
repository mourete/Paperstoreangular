import { DocumentoInstanciaSave } from './documento-instancia-save';

describe('DocumentoInstanciaSave', () => {
  it('should create an instance', () => {
    expect(new DocumentoInstanciaSave()).toBeTruthy();
  });
});
