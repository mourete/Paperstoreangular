import { ListVerPerfilUsuario } from './list-verPerfil-usuario';

describe('ListVerPerfilUsuario', () => {
  it('should create an instance', () => {
    expect(new ListVerPerfilUsuario()).toBeTruthy();
  });
});
