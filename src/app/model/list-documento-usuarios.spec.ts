import { ListDocumentoUsuarios } from './list-documento-usuarios';

describe('ListDocumentoUsuarios', () => {
  it('should create an instance', () => {
    expect(new ListDocumentoUsuarios()).toBeTruthy();
  });
});
