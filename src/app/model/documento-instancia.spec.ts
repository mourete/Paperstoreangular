import { DocumentoInstancia } from './documento-instancia';

describe('DocumentoInstancia', () => {
  it('should create an instance', () => {
    expect(new DocumentoInstancia()).toBeTruthy();
  });
});
